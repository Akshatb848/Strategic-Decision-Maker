# Heavy dependencies — installed in a separate Docker layer so they are
# cached independently. This layer only rebuilds when litellm version changes,
# which is rare. All other requirement changes remain fast.
litellm>=1.50.0
