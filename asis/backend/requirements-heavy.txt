# Heavy deps layer — intentionally empty.
# litellm removed: all LLM calls now go directly to Groq via httpx (no proxy needed).
# The Dockerfile no longer installs this file; kept to avoid breaking existing git history.
